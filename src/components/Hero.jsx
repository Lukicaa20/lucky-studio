const Hero = () => {
  return (
    <section className="hero-intro">
      <div className="hero-intro__content page-content">
        <p className="hero-intro__eyebrow">Lucky Studio</p>
        <h1>Design it. Slice it. Print it. Improve it.</h1>
        <p>From idea to iteration, every layer moves the project forward.</p>
      </div>
    </section>
  );
};

export default Hero;
